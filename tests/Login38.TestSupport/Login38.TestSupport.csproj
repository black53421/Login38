<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <IsPackable>false</IsPackable>
    <UseWPF>true</UseWPF>
  </PropertyGroup>

  <ItemGroup>
    <!-- The harness has to show windows under the same theme the applications ship, or a
         width measured here is a width from a different set of templates. -->
    <ProjectReference Include="..\..\src\Login38.Ui\Login38.Ui.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="WPF-UI" />
  </ItemGroup>

</Project>
